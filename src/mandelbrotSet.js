function MandelbrotSet(a, b) {
    const maxIterations = 100;
    let iteration = 0;
    let n = 0;
    let z = 0;

    while (iteration < maxIterations) {
        let real = a * a - b * b;
        let imagined = 2 * a * b;
    }

    // function ratioConvert(x, y) {
    //     let a = map(x, 0, width, -2, 2);
    //     let b = map(y, 0, height, -2, 2);
    // }
}
